# Man: tigrc

# Note: The editor tig uses is $GIT_EDITOR.
# Note: for bool-values, set either "1", "true" or "yes".

source ~/.config/tig/view.tigrc
source ~/.config/tig/keybind.tigrc

set wrap-lines = true
# set line-graphics = utf-8
set tab-size = 4

set git-colors = no
color cursor black green

# vim:syn=tmux
