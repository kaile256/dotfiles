-- TOML: fold.toml
-- Repo: lewis6991/foldsigns.nvim

-- Note: You can get sign names by `sign_getplaced(bufnr(), {'group': '*'})`
require('foldsigns').setup {
  -- include = nil, -- List of Lua patterns, nil to any signs.
  -- exclude = nil, -- List of Lua patterns.
}
