"From: tool.toml

let g:sonictemplate_vim_template_dir = [
      \ '$HOME/.config/nvim/data.d/sonictemplate'
      \]

let g:sonictemplate_vim_vars = {
      \ '_': {
      \   'author': 'Tsubasa TAKEMURA',
      \ },
      \}
